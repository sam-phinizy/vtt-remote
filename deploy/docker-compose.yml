# VTT Remote - Docker Compose
#
# Usage:
#   docker compose up -d
#
# With Traefik (recommended for SSL):
#   docker compose --profile traefik up -d

services:
  relay:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: vtt-relay
    profiles:
      - standalone
    restart: unless-stopped
    ports:
      - "80:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      retries: 3

  # Optional: Traefik for automatic SSL with Let's Encrypt
  traefik:
    image: traefik:v3.0
    container_name: vtt-traefik
    profiles:
      - traefik
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    command:
      - "--api.insecure=false"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
      - "--certificatesresolvers.letsencrypt.acme.email=${ACME_EMAIL:-admin@example.com}"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik-certs:/letsencrypt
    labels:
      - "traefik.enable=true"

  # Relay with Traefik labels (use this instead of relay when using traefik profile)
  relay-traefik:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: vtt-relay-traefik
    profiles:
      - traefik
    restart: unless-stopped
    ports:
      # Direct access on 8080 for local HTTP Foundry instances (ws://)
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/health"]
      interval: 30s
      timeout: 3s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.vtt.rule=Host(`${VTT_DOMAIN:-vtt-remote.example.com}`)"
      - "traefik.http.routers.vtt.entrypoints=websecure"
      - "traefik.http.routers.vtt.tls.certresolver=letsencrypt"
      - "traefik.http.services.vtt.loadbalancer.server.port=8080"

  # Documentation site (MkDocs Material)
  docs:
    build:
      context: ../docs
      dockerfile: Dockerfile
    container_name: vtt-docs
    profiles:
      - traefik
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.docs.rule=Host(`${DOCS_DOMAIN:-docs.arcanegrimoire.com}`)"
      - "traefik.http.routers.docs.entrypoints=websecure"
      - "traefik.http.routers.docs.tls.certresolver=letsencrypt"
      - "traefik.http.services.docs.loadbalancer.server.port=80"

volumes:
  traefik-certs:
